{
  "name": "@podman-desktop/extension-worker",
  "version": "0.0.1",
  "license": "apache-2.0",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@podman-desktop/api": "workspace:*",
    "vitest": "^3.1.3",
    "vite-plugin-dts": "^4.5.3"
  },
  "scripts": {
    "build": "vite build",
    "test": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest watch --coverage --passWithNoTests"
  }
}
